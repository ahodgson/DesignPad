<h1>New concept_category</h1>

<% if @concept_category %>
		<%= errors_for @concept_category %>
<% end %>

<% form_for(:concept_category, :url => concept_categories_path) do |f| %>
  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>

<%= link_to 'Back', concept_categories_path %>